# Tests

Unit tests for the Python modules and the POSIX-sh scripts. Everything runs under **pytest** (the shell tests shell out to `sh`), so one command runs the whole suite.

## Run

```sh
./tests/run.sh              # = .venv/bin/python -m pytest
./tests/run.sh -k dnsroute  # a subset (args pass through to pytest)
uv run pytest               # equivalent, if you use uv
```

Requires the project venv (`uv sync`). No root, no network, and no real routes/DNS are touched — `/sbin/route` is mocked, the resolver sweep is pointed at a temp dir, and the server-reachability probe's `nc` is replaced with a stub via `NC_BIN`, and `launchctl` is stubbed (via `LAUNCHCTL`) for the agent-load test.

## Layout

| File | Covers |
|---|---|
| `test_dnsroute.py` | `src/dnsroute.py`: route injection, dedup, failure cooldown, the in-flight fork-storm guard, SVCB hints, servfail-vs-silence. |
| `test_loadconfig.py` | `src/loadconfig.py`: the `via_vpn` classifier (rejecting metacharacter/typo entries), hostname/IP validators, the CLI (required/unknown keys, shell-quoting), the `reconnect_timeout` bounds (atoi-overflow guard; an absent key must emit nothing), and a `config.example.toml` drift check. |
| `test_shell.py` | `lib/common.sh` (`server_hostport` parsing incl. IPv6 literals; `wait_for_server` happy-path / timeout / recovery / skip against a stubbed `nc`; `recovery_budget` mode selection and the `NET_WAIT_MAX >= THROTTLE_INTERVAL` invariant; `pid_argv_has`, the shared per-PID argv re-confirm), `bin/vpnc-slice` (`@server` expansion + unsafe-domain filtering, the self-gated resolver sweep incl. the keep-list and marker isolation, the proxy-ownership invariant `_may_touch_proxy` / `_is_dnsroute` and the self-gated `_clear_proxy_state` / failed-bind teardown), `install-autostart.sh` (`dir_is_safe` + `verify_safe_ancestors` for the NOPASSWD-helper path, `dir_ok_for_repo` + `verify_repo_ancestors` for the user's repo-path ancestor walk, `verify_repo_interior`'s recursive interior vet, `load_agent`'s verify-and-retry launchd load (retries the transient bootstrap EIO and confirms via `launchctl print`, so a failed load fails closed instead of a false "loaded"), and the executed-with-a-leaked-seam-var refusal), and `bin/openconnect-auto-sso` both via the `OC_CONNECT_TEST=1` seam (`recovery_budget` wiring through `OC_LAUNCHD`, `_shq` quoting, the Phase-2 `-s` / `_build_vs` assembly, the PID-scoped `_end_browser` helper reaping incl. the PID-reuse guard, and `_mtu_degraded`'s MTU-collapse detection — degraded / healthy / unreachable, plus set-`u` safety with `PING_BIN` unset — against a `PING_BIN` stub, plus a structural check that the keepalive loop heals a confirmed collapse via the teardown helper and breaks only when that teardown succeeded) and end-to-end via a **stub-openconnect integration** (a stub `openconnect` + `sudo` + `nc` on PATH exercising the real `set -eu` Phase-1 PID-capture flow: cookie capture -> Phase 2, SIGTERM reaping both processes, the slow-but-live vs dead-helper backstop, the MTU-heal supervised-mode gate — active under `OC_LAUNCHD=keepalive`, refused when nothing would respawn a healed tunnel — Phase 2's signal contract (a **process-group** SIGTERM, which is what launchd and Ctrl-C both send, reaps openconnect and exits 143; the script's own trap cannot preempt that, since `sh` defers a trap until the foreground pipeline ends), and the executed-with-a-leaked-seam-var refusal). A metacharacter-laden `$PROJ` now sources fine (the old ERE guard is gone -- reaping matches fixed literals via `grep -F`). macOS-only (skipped elsewhere: BSD `stat -f`, `/etc/resolver`). |
| `test_vpn_browser.py` | `src/vpn_browser.py` pure helpers: `is_host_loopback` / `is_callback` (loopback-shape detection incl. `::1`, rejecting `127.evil.com` look-alikes), `is_allowed_url` (http/https logins only), `parse_callback` + `env_int` (malformed / int32-overflow-safe parsing), `_comm_is_openconnect` (parent-identity gate), and `reveal_decision` (reveal-after-N-idle-rounds fallback). Pure logic only — no QApplication, GUI, or network. |

## How the shell tests reach the functions

`lib/common.sh` is the easy case: it is constants and functions with no main body (side-effect-free at source time), so it needs no guard at all and the tests source it directly.

The executable scripts are different — they run their real logic on `exec`/dispatch, so tests can't just run them. Each has a test-only seam, all unreachable on the privileged path: the `OC_PROJ`/`RESOLVER_DIR` overrides are read only when NOT root (`bin/vpnc-slice`) or only under `OC_INSTALL_TEST=1` (`install-autostart.sh`), so a leaked env var can't redirect the root-run code — independent of any sudoers `env_keep`. (Sudo also strips the env, and every override defaults to the real value; that is now just defense in depth.)

- `OC_VPNC_SLICE_TEST=1` / `OC_INSTALL_TEST=1` / `OC_CONNECT_TEST=1` — source-guards that stop each script before its main body, leaving only the functions (and, for the connect script, the pure helpers `_shq` / `apply_launch_budget` / `_build_vs` plus the run-scoped reaper `_end_browser` and the `_HELPER_FULL`/`_P1_FULL` literals) defined, with no config load / auth / sudo. Each script also *refuses to run* if its seam var leaks into a real EXECUTED run (keyed on the resolved script basename, so a renamed symlink can't bypass it) — a leaked seam var must never silently no-op the tool.
- `OC_PROJ` — points `$PROJ`/`$proj` at the repo when sourced (normally derived from `$0`). Honored only off the privileged path: `bin/vpnc-slice` reads it only when NOT root (`id -u` != 0), and `install-autostart.sh` / `bin/openconnect-auto-sso` only under their `OC_INSTALL_TEST=1` / `OC_CONNECT_TEST=1` seam.
- `RESOLVER_DIR` — where `bin/vpnc-slice` reads/writes resolver files (default `/etc/resolver`); tests point it at a temp dir. Honored only when NOT root; a root `bin/vpnc-slice` forces `/etc/resolver` and ignores the override.
- `NC_BIN` — the binary `server_reachable` (in `lib/common.sh`) runs for its TCP probe, default `/usr/bin/nc`. Tests point it at a stub script in a temp dir, so `wait_for_server` is exercised without ever touching the network. It is an override only because the probe runs as the user — root's `bin/vpnc-slice` never calls it.
- `PING_BIN` — the binary `bin/openconnect-auto-sso` runs for the keepalive ping AND the MTU-collapse probes (`_mtu_degraded`), default `/sbin/ping`. Tests point it at a `$MODE`-driven stub (degraded / healthy / unreach) so `_mtu_degraded` is exercised without pinging anything. It defaults inside `_mtu_degraded` as well as with the other env vars, since that block sits inside the `OC_CONNECT_TEST` seam — a sourced caller under `set -u` would otherwise trip on it (one test deliberately unsets it and probes loopback with the real `/sbin/ping`). Like `NC_BIN` it is an override only because the keepalive loop runs as the user; the advanced env-only knobs it works with (`MTU_PROBE_SIZE`, `MTU_HEALTHCHECK_FAILS`, `MTU_HEALTHCHECK_BASELINE_TRIES`) are not config keys.
- `LAUNCHCTL` / `LOAD_RETRY_SLEEP` — the `launchctl` binary `load_agent` (in `install-autostart.sh`) drives to (re)load the LaunchAgent, and its retry backoff. Honored only under `OC_INSTALL_TEST=1`, so a real install always uses the real `launchctl`; tests point `LAUNCHCTL` at a stub that fails `bootstrap` with a transient EIO then succeeds, with `LOAD_RETRY_SLEEP=0` so the retry loop runs instantly.

`libexec/vpn-teardown` is intentionally left hermetic (no seam — it's the root-owned NOPASSWD helper); it's tested only as a black-box subprocess with a PATH-stubbed `pgrep`, and its kill path is not exercised (it would signal real PIDs).
